# AgentSociety Mini Reinterpretation - Pinned Dependencies
#
# SECURITY: All versions pinned with >= min and < major-next to prevent
# supply-chain surprises while allowing security patches.
# For maximum reproducibility use: pip install --require-hashes -r requirements-lock.txt
#
# Context: litellm supply-chain attack (March 2026) — this project uses
# google-genai directly to eliminate that risk.

# === Core: LLM Backends ===
# Track 1 — Gemini remote API (google-genai SDK)
google-genai>=1.72.0,<2.0.0
# Track 2 — OpenAI-compatible endpoints (vLLM / HF Router / Mistral / Ollama)
# httpx is already transitively required by google-genai; pinning it here makes
# the openai_compat client's dependency explicit.
httpx>=0.28.0,<1.0.0

# === Core: Data & Config ===
pydantic>=2.12.0,<3.0.0
json-repair>=0.59.0,<1.0.0
python-dotenv>=1.2.0,<2.0.0

# === Storage ===
sqlalchemy>=2.0.49,<3.0.0

# === UI ===
streamlit>=1.56.0,<2.0.0
plotly>=6.7.0,<7.0.0
pyvis>=0.3.2,<1.0.0

# === Async ===
aiohttp>=3.13.0,<4.0.0
